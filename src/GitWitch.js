const { } = require()
const { } = require()
const

class GitWitch {
  constructor() {
        process() {

        }
  }
  // parser: this.parser,
  // runner: this.runner,
  // formatter: this.formatter
}

module.exports = GitWitch;
