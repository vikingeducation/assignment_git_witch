class CommandRunner {
  constructor(api) {
    this.api = api;
  }
  run(command) {
    // return new Promise((resolve, reject) => {
    //   resolve("something");
    // });
  }
}

module.exports = CommandRunner;
